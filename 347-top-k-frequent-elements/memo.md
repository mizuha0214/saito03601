# step1
heapと出現頻度の対応付けが分からなかった（まずCountすることを思いつかなかった）。
方針をgptに聞いて実装。

# step2
- 命名を修正
- 計算量
n=配列全体の要素数、m=ユニーク要素数、k=欲しい上位件数
O(n+k*log m) vs O(n+m*log k)
なのでkとmの大きさ次第。

# step3
元データ >> 抽出対象データ　という状況の方が多そうなので、O(n+m*log k)で書く。

